### ✅ Readme: 
Create a README file to answer the following question-

 - What is JSX, and why is it used?
    jsx = JavaScript XML which is used in react which describes wha the ui should look like

 - What is the difference between State and Props?
   state= used to store value and change easily
   props = it is input which is passed from parent to child components

 - What is the useState hook, and how does it work?
    it is a react hook that allows funcitonal components to have state

 - How can you share state between components in React?
    by using props

 - How is event handling done in React?
   cosnt handleEvnet =()=>{
    alert('hello')
   }
    <button onClick={handleEvent}>Click Me</button> like this



